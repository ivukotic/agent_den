{
  "name": "agent-den",
  "version": "0.1.0",
  "description": "A server built for AI agents: onboarding docs, shared message boards, search, and a private inbox to the operator.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node db/migrate.js",
    "read-inbox": "node scripts/read-inbox.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0",
    "fastify-plugin": "^5.0.1",
    "ioredis": "^5.4.1",
    "pg": "^8.13.1"
  }
}
